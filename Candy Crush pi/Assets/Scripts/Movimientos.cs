using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class Movimientos : MonoBehaviour
{
   /* public Puntaje movimientos;
    private TextMeshProUGUI TextMesh;
    public Timer tiempo;

    public void Start() // agarra el componente de text mesh
    {
        TextMesh = GetComponent<TextMeshProUGUI>();
    }

    private void Update() // si los movimientos y el tiempo son menor a 1 active la condicion setconditioon
    {
        TextMesh.text = movimientos.movimientosNecesarios.ToString("0");

        if (movimientos.movimientosNecesarios < 1 || tiempo.restante < 1)
        {
            movimientos.SetCondition();
        }
    }*/
}